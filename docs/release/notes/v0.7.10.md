# v0.7.10 — 2026-06-23

## 버그 수정
- **삭제한 파일이 사이드바에 남던 문제 (재발)** — `shell.trashItem` 성공 직후 Pane/NotePane의 디바운스 autosave가 같은 경로에 파일을 다시 써서, 탭은 닫히는데 목록엔 남던 레이스. `useSession.deletingPaths` 가드로 삭제 중인 경로엔 autosave가 쓰지 않도록 차단.
