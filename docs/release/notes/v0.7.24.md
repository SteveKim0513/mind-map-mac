# v0.7.24 — 2026-07-06

## 새 기능

- **이중 AI 프로바이더 지원.** 설정(⌘,) → AI 기능에서 Claude(Anthropic)와 GPT(OpenAI) API 키를 각각 등록할 수 있습니다. 두 키가 모두 있으면 라디오 버튼으로 활성 모델을 전환합니다. 하나만 등록하면 자동 활성화됩니다.
- **API 키 발급 링크 내장.** 각 프로바이더 카드에서 키 발급 페이지(console.anthropic.com, platform.openai.com)를 바로 열 수 있습니다.

## 내부

- 디자인 원칙 문서(`docs/design/UI-DESIGN-PRINCIPLES.md`) + `/design-ui` 스킬 추가
- 디자인 하네스(`scripts/harness/check-design.mjs`) — raw hex 색상·폰트 크기 자동 검사
- `shell:openExternal` IPC 채널 추가 (https:// URL만 허용)
