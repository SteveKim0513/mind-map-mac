# v0.7.19 — 2026-06-26

## 신규

- **⌘W로 탭을 닫을 수 있습니다.**

## 개선

- **링크로 노트 가져올 때 같은 이름 노트가 이미 있으면 기존 노트로 이동합니다.** 중복 파일이 생기지 않습니다.
- **파일 이름을 바꿀 때 같은 이름이 있으면 알려줍니다.** 기존에는 조용히 "이름 2"로 만들었는데, 이제 "이름의 파일이 이미 있습니다" 메시지가 표시됩니다.
